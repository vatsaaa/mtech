# Requirements for Legal Information Retrieval System
# Install these packages using: pip install -r requirements.txt

# Core Python libraries (built-in - no installation needed)
# - os, re, csv, json, argparse, time, collections, typing, warnings

# Core data processing (optional for advanced features)
pandas>=1.3.0
numpy>=1.21.0

# Document processing (optional for document analysis)
PyPDF2>=2.10.0
python-docx>=0.8.11
openpyxl>=3.0.9

# Text processing and NLP (optional for advanced text analysis)
nltk>=3.7
textblob>=0.17.1

# Web scraping (optional for dataset collection)
requests>=2.28.0
beautifulsoup4>=4.11.0

# Data visualization (optional for analysis plots)
matplotlib>=3.5.0
seaborn>=0.11.0

# File handling (optional for advanced file operations)
chardet>=4.0.0
xlrd>=2.0.1

# Progress bars and utilities (optional)
tqdm>=4.64.0

# JSON handling (optional - Python's built-in json module is sufficient)
ujson>=5.4.0

# Note: The legal_spell_checker.py application only uses built-in Python libraries
# and does not require any external dependencies to run the core functionality.
